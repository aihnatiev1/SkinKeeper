---
phase: 16-multi-account-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/auth.ts
  - backend/src/routes/inventory.ts
  - backend/src/routes/__tests__/auth.test.ts
  - backend/src/routes/__tests__/inventory.test.ts
autonomous: true
requirements:
  - ACCT-05
  - ACCT-03

must_haves:
  truths:
    - "POST /auth/accounts/link returns 403 with error: premium_required for a free user who already has 1 linked account"
    - "POST /auth/accounts/link returns 200 for a premium user regardless of how many accounts they have"
    - "GET /api/inventory without ?accountId returns items from ALL user accounts including account_avatar_url field"
  artifacts:
    - path: "backend/src/routes/auth.ts"
      provides: "Premium gate active on /accounts/link endpoint"
      contains: "isPremium"
    - path: "backend/src/routes/inventory.ts"
      provides: "Multi-account inventory query including avatar_url"
      contains: "account_avatar_url"
    - path: "backend/src/routes/__tests__/auth.test.ts"
      provides: "Integration tests for ACCT-05 premium gate"
      contains: "premium_required"
    - path: "backend/src/routes/__tests__/inventory.test.ts"
      provides: "Integration tests for multi-account inventory merge"
      contains: "account_avatar_url"
  key_links:
    - from: "backend/src/routes/auth.ts"
      to: "users.is_premium"
      via: "pool.query SELECT is_premium"
      pattern: "SELECT is_premium FROM users WHERE id"
    - from: "backend/src/routes/inventory.ts"
      to: "steam_accounts.avatar_url"
      via: "JOIN steam_accounts sa ON i.steam_account_id = sa.id"
      pattern: "sa\\.avatar_url"
---

<objective>
Activate the backend premium gate on account linking and add avatar_url to the inventory response — two surgical backend changes that unblock the Flutter multi-account UI.

Purpose: Free users must be blocked at the API layer when trying to link a second account. Item cards need avatar_url to render account badges without deriving CDN URLs client-side.
Output: Premium gate live in auth.ts, avatar_url in every inventory item response, integration tests covering both behaviors.
</objective>

<execution_context>
@/Users/abs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-multi-account-gap-closure/16-RESEARCH.md
</context>

<interfaces>
<!-- Key interfaces extracted from codebase for executor context. -->

From backend/src/routes/auth.ts (lines 214–256) — POST /accounts/link:
The premium gate block (lines 217–236) is entirely written but commented out with
"// TODO: re-enable premium gate after testing". The block queries `is_premium` from
`users` table, then counts rows in `steam_accounts` where `user_id = $1`. If free user
has >= 1 account, returns 403 `{ error: "premium_required", message: "Upgrade to Premium..." }`.

From backend/src/routes/inventory.ts (lines 23–34) — SELECT query:
```sql
SELECT i.asset_id, i.market_hash_name, i.icon_url, i.wear,
       i.float_value, i.rarity, i.rarity_color, i.tradable,
       i.trade_ban_until,
       i.inspect_link, i.paint_seed, i.paint_index, i.stickers, i.charms,
       sa.steam_id as account_steam_id,
       sa.id as account_id,
       sa.display_name as account_name
FROM inventory_items i
JOIN steam_accounts sa ON i.steam_account_id = sa.id
WHERE sa.user_id = $1${accountFilter}
ORDER BY i.market_hash_name
```
Missing: `sa.avatar_url as account_avatar_url`

From backend/src/routes/__tests__/auth.test.ts — file exists (created in phase 15).
Test pattern uses `createTestApp()` + supertest. Mock pool with vi.mock.

From backend/src/routes/__tests__/inventory.test.ts — file DOES NOT exist yet.
Must be created. Follow pattern from auth.test.ts.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Uncomment premium gate in auth.ts + add ACCT-05 test cases</name>
  <files>backend/src/routes/auth.ts, backend/src/routes/__tests__/auth.test.ts</files>
  <behavior>
    - Test: POST /auth/accounts/link with mocked pool returning is_premium=false and existing account count=1 → 403 { error: "premium_required" }
    - Test: POST /auth/accounts/link with mocked pool returning is_premium=false and account count=0 → 200 (link URL returned)
    - Test: POST /auth/accounts/link with mocked pool returning is_premium=true and account count=5 → 200 (no gate for premium)
  </behavior>
  <action>
    In backend/src/routes/auth.ts lines 217–236: remove the comment markers from the entire premium gate block. The block is already correctly written — just uncomment it. Remove the "// TODO: re-enable premium gate after testing" comment line above it.

    In backend/src/routes/__tests__/auth.test.ts: add three new test cases inside the existing describe block for the accounts/link route (or create a new describe('POST /auth/accounts/link — premium gate') block). Mock pool.query using vi.fn():
    - Case 1 (free user, has account): First pool.query call returns `[{ is_premium: false }]`, second returns `[{ cnt: 1 }]` → expect 403 with body `{ error: "premium_required" }`.
    - Case 2 (free user, no accounts yet): First returns `[{ is_premium: false }]`, second returns `[{ cnt: 0 }]` → expect 200.
    - Case 3 (premium user): First returns `[{ is_premium: true }]` → expect 200 (second query not called).

    IMPORTANT: vi.mock factory hoisting issue (decision [15-04]) — use inline mock values, not top-level variable references. Pool mock pattern: `vi.mocked(pool.query).mockResolvedValueOnce({ rows: [...] } as any)` for sequential calls.

    Run tests RED first (uncomment gate = tests may already pass — verify gate is actually called by checking the 403 case fails WITHOUT the uncomment, then passes WITH it).
  </action>
  <verify>
    <automated>cd /Users/abs/ideaProjects/skinkeeper/backend && npm test -- --run src/routes/__tests__/auth.test.ts</automated>
  </verify>
  <done>All existing auth tests still pass. Three new premium gate test cases pass: 403 for free+existing, 200 for free+none, 200 for premium.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add avatar_url to inventory query + create inventory.test.ts</name>
  <files>backend/src/routes/inventory.ts, backend/src/routes/__tests__/inventory.test.ts</files>
  <behavior>
    - Test: GET /api/inventory (no accountId filter) returns items array where each item has account_avatar_url field
    - Test: GET /api/inventory?accountId=2 still works (filters to one account) and items still have account_avatar_url
    - Test: GET /api/inventory returns 200 with { items: [...], count: N }
  </behavior>
  <action>
    In backend/src/routes/inventory.ts, update the SELECT query to add `sa.avatar_url as account_avatar_url` after `sa.display_name as account_name`:

    ```sql
    sa.steam_id as account_steam_id,
    sa.id as account_id,
    sa.display_name as account_name,
    sa.avatar_url as account_avatar_url
    ```

    Create backend/src/routes/__tests__/inventory.test.ts following the exact pattern of auth.test.ts (vi.mock for pool, createTestApp, supertest). Mock pool.query to return two responses: the inventory SELECT (returns mock items array with account fields including avatar_url), and getLatestPrices (mock the prices service or mock its pool calls).

    If getLatestPrices is difficult to mock (it calls pool directly), use vi.mock('../../services/prices.js', () => ({ getLatestPrices: vi.fn().mockResolvedValue(new Map()) })) at the top of the test file.

    Test structure:
    ```typescript
    describe('GET /api/inventory', () => {
      it('returns all accounts items when no accountId filter', async () => { ... })
      it('includes account_avatar_url in each item', async () => { ... })
      it('filters by accountId when provided', async () => { ... })
    })
    ```

    Mock pool.query for inventory: return `{ rows: [{ asset_id: 'abc', market_hash_name: 'AK-47 | Redline (FT)', account_id: 1, account_steam_id: '76561198000000001', account_name: 'TestUser', account_avatar_url: 'https://avatars.steamstatic.com/abc_medium.jpg', ... }] }`.
  </action>
  <verify>
    <automated>cd /Users/abs/ideaProjects/skinkeeper/backend && npm test -- --run src/routes/__tests__/inventory.test.ts</automated>
  </verify>
  <done>inventory.test.ts passes all 3 cases. GET /api/inventory response includes account_avatar_url on each item. Existing backend tests unaffected: `cd /Users/abs/ideaProjects/skinkeeper/backend && npm test -- --run` exits 0.</done>
</task>

</tasks>

<verification>
Full backend suite passes after both tasks:
```
cd /Users/abs/ideaProjects/skinkeeper/backend && npm test -- --run
```
All tests exit 0. New test files cover ACCT-05 (premium gate 403/200) and ACCT-03 (account_avatar_url in response).
</verification>

<success_criteria>
1. `POST /auth/accounts/link` returns 403 `{ error: "premium_required" }` for free user with >= 1 account
2. `POST /auth/accounts/link` returns 200 for free user with 0 accounts or any premium user
3. `GET /api/inventory` response includes `account_avatar_url` on every item
4. `backend/src/routes/__tests__/inventory.test.ts` exists with 3 passing test cases
5. `cd backend && npm test -- --run` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/16-multi-account-gap-closure/16-01-SUMMARY.md`
</output>
